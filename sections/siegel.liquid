<div class="SectionGrid color-{{ section.settings.color_scheme }}" style="max-height: 320px; overflow-hidden;">
  {% unless section.settings.backgroundImage == blank %}
    <img src="{{ section.settings.backgroundImage | image_url: width: 2560 }}" alt="{{ section.settings.backgroundImage.alt }}" width="2560" height="1104" loading="lazy" class="w-100p h-100p" style="grid-column: 1 / span 5; grid-row: 1 / span 1;">
  {% endunless %}
  <div class="siegel-content" style="padding: 60px 0 0; grid-column: 2 / span 3; grid-row: 1 / span 1;">
    <div class="siegel-badges" style="display: flex; flex-direction: row; flex-wrap: nowrap; justify-content: space-between; align-items: flex-start; margin-bottom: 26px;">
      {%- for badge in section.blocks -%}
        <img src="{{ badge.image | image_url: width: 750 }}" alt="{{ badge.image.alt }}" width="200" height="200" loading="lazy" class="siegel-image">
      {%- endfor -%}
    </div>
  <div style="text-align: center;">
    {{ section.settings.subline }}
  </div>
  </div>
</div>

{% schema %}
{
  "name": "Siegel",
  "tag": "section",
  "settings": [
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "background-1",
          "label": "t:sections.image-banner.settings.color_scheme.options__3.label"
        },
        {
          "value": "background-2",
          "label": "t:sections.image-banner.settings.color_scheme.options__4.label"
        },
        {
          "value": "inverse",
          "label": "t:sections.image-banner.settings.color_scheme.options__5.label"
        }
      ],
      "default": "inverse",
      "label": "t:sections.image-banner.settings.color_scheme.label"
    },
    {
      "type": "image_picker",
      "id": "backgroundImage",
      "label": "Hintergrundbild"
    },
    {
      "type": "text",
      "id": "subline",
      "label": "Anmerkung",
      "default": "* Umfrageergebnis von sleep.ink® Studie mit 200 Teilnehmer*innen, März 2021"
    }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Siegel",
      "limit": 3,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Bild"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Siegel",
      "settings": {
        "color_scheme": "inverse",
        "subline": "* Umfrageergebnis von sleep.ink® Studie mit 200 Teilnehmer*innen, März 2021"
      },
      "blocks": [
        {
          "type": "badge"
        },
        {
          "type": "badge"
        },
        {
          "type": "badge"
        }
      ]
    }
  ]
}
{% endschema %}